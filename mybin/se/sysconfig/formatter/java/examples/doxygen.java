/*!
 *  
 *  
 * Function summary 
 *  
 * \author Jimbo 
 * \param charchar It's a character
 * \param b A character count
 * \param a A non-null string that is bigger than a pin, but
 *          smaller than a breadbox.
 * \return a long. 
 * 
 * \code{.java}
 *     long rv=boid('X',
 *      0, 
 *      "smurfy");
 * \endcode 
 */
long boid(char charchar, int b, String a)
{
   return blarp(c, norm(b, a));
}
