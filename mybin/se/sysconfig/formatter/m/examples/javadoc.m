/**
 *  
 *  
 * Function summary 
 * @author Jimbo 
 * @param charchar It's a character
 * @param b A character count
 * @param a A non-null string that is bigger than a pin, but
 *          smaller than a breadbox.
 * @return a long. 
 * @example 
 * <pre> 
 *     long rv=boid('X',
 *      0, 
 *      "smurfy");
 * </pre> 
 */
long boid(char charchar, int b, char const *a)
{
   return blarp(c, a + b);
}
