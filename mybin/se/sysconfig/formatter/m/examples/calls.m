int boid(std::string& s, float& fw)
{
    size_t sz = s.size();

    init();
    s.resize(256, '@');
    populate(s,sz);
   
    return operator+(sz, pred(12)); 
}
template<class K, class V>
map<K,V> make_map();

void munge(id *other)
{
    map<int, float> tvals = make_map<int,float>();

    [other munge: tvals, 1, 2, 3];
}
